# Environment variables template
#
# FOR DESKTOP SESSIONS:
# Copy this to .env.local and fill in your values
# Then run: source ./load-env.sh
#
# FOR MOBILE/WEB SESSIONS:
# Configure these as environment variables in Claude Code settings
# The SessionStart hook will automatically load them
#
# Note: .env.local is gitignored and persists across desktop sessions
# but is NOT available in mobile/web cloud sessions

# Supabase credentials (for production)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Anthropic API key
ANTHROPIC_API_KEY=your_anthropic_api_key

# GitHub token (for CLI operations like gh issue, gh pr)
GITHUB_TOKEN=your_github_token
